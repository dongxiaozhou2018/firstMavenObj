<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dxz">
<!--employees_id,first_name,last_name,email,phone_number,salary,commission_pct,manager_id,department_id,hiredate-->
    <!--查询-->
    <select id="SelectAllBoy" resultType="com.neuedu.jdbc.mybatisDemo.Employees">
        select *
        from employees
    </select>

    <!--增加-->
    <insert id="AddOneBoy" parameterType="com.neuedu.jdbc.mybatisDemo.Employees">
        INSERT into employees(employee_id,first_name,last_name,email,phone_number,job_id,salary,commission_pct,manager_id,department_id,hiredate)
        VALUES(#{employee_id},#{first_name},#{last_name},#{email},#{phone_number},#{job_id},#{salary},#{commission_pct},#{manager_id},#{department_id},#{hiredate});
    </insert>

    <!--删除-->
    <delete id="deleteOne" parameterType="int">
        delete from employees where employee_id =#{employee_id}
    </delete>

    <!--修改-->
    <update id="updateOne" parameterType="com.neuedu.jdbc.mybatisDemo.Employees">
        update employees set first_name=#{first_name},last_name=#{last_name},email=#{email},phone_number=#{phone_number},job_id=#{job_id},salary=#{salary},commission_pct=#{commission_pct},manager_id=#{manager_id},department_id=#{department_id},hiredate=#{hiredate}where employee_id = #{employee_id}
    </update>
</mapper>